### JQuery плагин для валидации и форматирования поля ввода денежных значений

Не имеет никаких настроек, просто работает и все

Используется вот так

```javascript
$(selector).validMoney();
```

Не забываем подключить библиотеку jquery.